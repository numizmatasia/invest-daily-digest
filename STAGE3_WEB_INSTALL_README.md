# Stage 3 browser installation

This branch was populated by `.github/workflows/INSTALL_STAGE3_FOUNDATION.yml`.
The workflow runs inside GitHub, tests the Stage 3 payload, and commits it only after all
checks pass. It does not modify `main.py` or `.github/workflows/daily.yml`.
